#include<iostream>
#include<stdio.h>
class student
{
	int roll_no:
	char name[20];
};
int main()
{
	student s;
	cout<<"Enter the roll_no ";
	cin>>s.roll_no;
	cout<<"Enter the name: ";
	gets(s.name)name;
	cout<<"roll_no: "<<s.roll_no;
	cout<<"\nname: ";
	puts(s.name);
}